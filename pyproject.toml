[project]
name = "world-cup"
version = "0.1.0"
description = "World cup scoring algorithm"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.3",
    "streamlit>=1.58.0",
]
